<div class="form-group">
	<label for="variable-select">Variables:</label>
	<select id="variable-select" class="form-control" multiple style="width: 100%">
		{{#each this}}
			<option value="{{@key}}">{{displayName}}</option>
		{{/each}}
	</select>
        </div>
    <div class="date-filter-container"></div>
    <div class="hint-div">
        <em>		
            Chosen variables will be added to the map and selected variables list below.
        </em>
    </div>